# Зависимости бэкенда RockCRM. Версии зафиксированы: расхождение версий
# psycopg между машиной разработчика и CI однажды уже стоило бы нам вечера
# на воспроизведение «у меня работает».

fastapi==0.115.6          # HTTP-слой и валидация запросов через pydantic v2
uvicorn==0.34.0           # ASGI-сервер для разработки и прогона тестов
psycopg[binary]==3.2.3    # драйвер PostgreSQL; binary — чтобы не собирать libpq на Windows
psycopg-pool==3.2.4       # пул соединений: соединение под ролью rockcrm_app дорого открывать на каждый запрос
pydantic==2.10.4          # модели ответов ровно по контракту, они же документация API
python-dotenv==1.0.1      # чтение backend/.env, чтобы строка подключения не жила в коде

# --- только для разработки и тестов ---
pytest==8.3.4             # тесты против настоящей базы
httpx==0.28.1             # TestClient FastAPI работает поверх httpx
