[pytest]
testpaths = tests
# tests/ лежит рядом с app/ и scripts/, поэтому корень бэкенда должен быть
# в пути импорта — иначе `from app import ...` не найдётся при запуске
# из любой другой директории.
pythonpath = . tests
addopts = -q --tb=short
