# Зависимости для прогона тестов схемы.

# Встраиваемый PostgreSQL: поднимает собственный кластер, чтобы тесты
# не требовали установленной базы. Сборка урезанная — без btree_gist,
# см. README.md.
pgserver>=0.1.4

# Драйвер. Нужен именно psycopg 3: тесты полагаются на diag.constraint_name,
# чтобы отличать «сработало нужное ограничение» от «упало по другой причине».
psycopg[binary]>=3.2
