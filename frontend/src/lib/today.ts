import { USE_MOCKS } from '../api';
import { todayIso } from './format';

/**
 * «Сегодня» интерфейса.
 *
 * В мок-режиме это день прототипа — 12 августа 2026: фикстуры расписания,
 * журнала и сроков абонементов привязаны к нему, и на реальной сегодняшней
 * дате все сроки выглядели бы истёкшими. На живом бэкенде берётся настоящая
 * дата браузера: эндпоинта «сегодня по времени школы» контракт не даёт.
 */
export const TODAY: string = USE_MOCKS ? '2026-08-12' : todayIso();
